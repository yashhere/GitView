import { FormControl } from '@angular/forms';

// function validateEmail(c: FormControl) {
//   const regexp = '/\B@([a-z0-9](?:-?[a-z0-9]){0,38})/gi';

//   return regexp.test(c.value) ? null : {
//     validateEmail: {
//       valid: false
//     }
//   };
// }
